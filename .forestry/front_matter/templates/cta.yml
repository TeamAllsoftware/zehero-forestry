---
label: cta
hide_body: true
fields:
- name: blockBGcolor
  type: select
  default: blanc
  config:
    required: false
    options:
    - gris
    - blanc
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Couleur de fond du composant
- name: marge
  type: select
  default: 40
  config:
    required: false
    options:
    - 20
    - 40
    - 60
    - 80
    - 100
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Quelle hauteur des marges supérieures et inférieures ?
- name: intro
  type: text
  config:
    required: true
    max: 50
  label: intro
  description: only one ligne of 50 characters
- name: headline
  type: text
  config:
    required: true
    max: 65
  label: headline
  description: Another text in bigger size to express more the intention of the "call
    to action", the button
- name: button
  type: field_group
  config: {}
  fields:
  - name: text_button
    type: text
    config:
      required: true
    label: Text on the button
  - name: href_button
    type: text
    config:
      required: true
    label: url for this button
  label: button
