---
label: counter + headline + content
hide_body: true
fields:
- name: counter
  type: text
  config:
    required: true
  label: counter
  description: Add number
- name: text_refers_to_counter
  type: text
  config:
    required: true
    max: 24
    min: 20
  label: text refers to counter
- name: title
  type: text
  config:
    required: true
  label: title
- name: headline
  type: text
  config:
    required: true
    max: 55
    min: 45
  label: headline
- name: content
  type: textarea
  default: ''
  config:
    required: true
    wysiwyg: false
    schema:
      format: markdown
    max: 250
    min: 200
  label: content
